# Raspberry Pi Setup

- Raspberry Pi OS Lite oder Ubuntu Server nutzen
- SSD bevorzugen
- Docker und Docker Compose installieren
- Repository klonen
- `.env` aus `.env.example` erzeugen
- `docker compose up --build` starten
